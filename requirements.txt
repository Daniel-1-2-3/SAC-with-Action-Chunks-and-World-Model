numpy<2

# Config loading only (elements.Config / elements.Flags) and configs.yaml.
elements>=3.17.0
ruamel.yaml

# JAX stack. NOT needed by training any more -- the world model is TD-MPC2 in
# PyTorch and nothing in the active code imports jax. These are here only for
# the vendored embodied/ (DreamerV3) tree, which nothing imports either.
# Safe to drop along with embodied/.
jax[cuda12]<0.7.0
optax
chex
einops
jaxtyping
ninjax>=3.5.1

# policy (PyTorch side) -- install from the CUDA index above, not plain PyPI,
# or pip serves a CPU-only wheel
torch

# environment
ogbench
mujoco
gymnasium

# rendering and plots
opencv-python
matplotlib

# embodied support (vendored tree only)
cloudpickle
msgpack
portal
rich

wandb[media]
imageio