numpy<2
elements>=3.17.0
ruamel.yaml
# PyTorch: install from the CUDA index (install.sh), NOT plain PyPI, or a
# CPU-only wheel may be served and every run prints "device: cpu".
# Never install jax[cuda12] next to torch: its nvidia-* wheels replace
# torch's CUDA libraries and torch.cuda.is_available() becomes False.
ogbench
mujoco
gymnasium
opencv-python
matplotlib
wandb
imageio
